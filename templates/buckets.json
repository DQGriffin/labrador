{
    "defaults": {
        "name": "Temp Storage Bucket",
        "region": "us-east-2",
        "versioning": true,
        "onDelete": "skip",
        "blockPublicAccess": true,
        "staticHosting": {
            "enabled": true,
            "indexDocument": "index.html",
            "errorDocument": "error.html"
        },
        "tags": {
            "app": "labrador",
            "env": "{{env}}"
        }
    },
    "buckets": [
        {
            "name": "{{env}}-{{project_name}}-assets-6",
            "onDelete": "delete",
            "staticHosting": {
                "enabled": false
            }
        }
    ]
}